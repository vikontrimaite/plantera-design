function showMessage() {
    secretMsg = document.getElementById('secret-msg');
    secretMsg.className = 'secret-box';

    secretBtn = document.getElementById('secret-btn');
    secretBtn.className = 'hidden';
}
